# SuiteCRM 19.0 testing

## Source and support boundary

SuiteCRM is not packaged by Debian Trixie. The appliance installs the official
SuiteCRM-Core 8.10.2 production archive from its GitHub release. The build pins
SHA-256
`ac4693d10ea2d6cd20b3256098781d134599d88878fb24e8dc127038d624f5a8`.
SuiteCRM documents PHP 8.4 and MariaDB 11.8 as supported for the 8.10 release
line. The official stable GitHub release feed supplies updater metadata and an
asset digest.

## Acceptance command

Run the repository through the isolated Wave 2 harness:

```sh
TKLDEV_CONTAINER=tkldev19-wave2 \
TKL_HARNESS_STATE_DIR=/home/agent/.local/state/turnkey-v19-harness-wave2 \
TKL_HARNESS_LOCK_FILE=/home/agent/.local/state/turnkey-v19-harness-wave2/build.lock \
/sandboxed-git/turnkey/tools/test-v19-appliance suitecrm \
  --source /home/agent/.local/worktrees/turnkey-apps/suitecrm/wish-suitecrm-v19-trixie
```

## README evidence crosswalk

| README contract | Test in `tests/v19.sh` | Expected result | Current result and evidence |
|---|---|---|---|
| Official SuiteCRM under `/var/www/suitecrm` | Read `VERSION`, require an absolute HTTPS site URL, load the trusted HTTPS root, and fetch a versioned JavaScript asset | Version 8.10.2 and successful application and asset responses | PASS: version, site URL, root, and asset checks completed in the retained run |
| SuiteCRM admin credential | Install a disposable random admin credential using SuiteCRM's hash format, perform the CSRF-protected JSON login, then read session status | Login succeeds and the active user is `admin` | PASS: login and active admin session were verified over HTTPS |
| CRM record workflow | Create and read an Account through SuiteCRM's BeanFactory, verify its MariaDB row, then delete it through BeanFactory | The same ID and name are returned and cleanup succeeds | PASS: Account create, read, and cleanup completed |
| MariaDB-backed application | Query the created Account through the local database | One live matching row exists before cleanup | PASS: the matching live Account row was present |
| Scheduled SuiteCRM jobs | Run `bin/console schedulers:run` as `www-data` and inspect `/etc/cron.d/suitecrm` | Supported scheduler tasks pass and the every-minute cron entry is present | PASS: scheduler tasks passed and the supported cron entry matched |
| Supervised stable updates | Run `suitecrm-update --check` | Installed and eligible versions, stable channel, status, URL, and official SHA-256 metadata are returned without changing the installation | PASS: installed and eligible version 8.10.2, stable channel, up-to-date status, and official digest metadata |
| Confconsole permissions control | Parse the shipped permissions helper and confirm its current SuiteCRM paths | Shell syntax is valid for the current helper | PASS: `bash -n` accepted the shipped helper |
| HTTPS, Adminer, Postfix, Webmin, SSH and core credentials | Cite the validated Core 19 and LAMP baseline for unchanged inherited services; exercise SuiteCRM HTTPS separately above | Inherited services retain their baseline behavior | SuiteCRM HTTPS and its Apache, MariaDB, and cron dependencies passed here; unchanged inherited services remain covered by the Core 19 and LAMP baseline |

## Retained Wave 2 evidence

The exact acceptance run `20260825t184703z-5952-27541` completed with
`verdict=PASS` on 2026-08-25. It tested source commit
`05ed4b4eda9010d5e0f898c45876d1f7d40f8fee` with these retained identities:

- Report SHA-256:
  `2fab153610452d93a09be909b33b0a8b575d2927594d6fae9551a6320d9b1af7`
- Source and transport archive SHA-256:
  `429ed09549336c70e8da19e6cb9b9a8ce09a992210c9f81755bfc8fd88d9a425`
- Input tree SHA-256:
  `850eb0ac4227ddd15d4a408ec5703e50092d0ee76341ab3b63e5fcc1602b3342`
- Build tree SHA-256:
  `04421c042c7f5bcc01f68cfc611ed81c0b0b99275afb4181b5dcb6b52d26f444`
- Harness commit:
  `0ea6097880e949cb316e9d3bc2fb32a5a4387cd6`
- Harness tree SHA-256:
  `8933b8111a08a4f0fb92de1b743dea4471b2e1fdc3679c1c7796a57560ba375c`

The retained report records `tkldev19-wave2`, the Wave 2 state directory and
lock file, the expected overlayfs `root.patched` fallback, healthy first boot,
successful runtime checks, cleanup verification, and the official updater
result `up-to-date; eligible=8.10.2`.

## Deferred issues

- The acceptance run performs a non-destructive stable-channel update check.
  Applying a future release remains a supervised administrator operation.
- Confconsole's interactive menu is not automated. Its helper paths and syntax
  are checked, while the main SuiteCRM workflow runs with the default build
  permissions.
- Docker acceptance does not exercise installer, kernel, or hardware behavior.
  The exact run checks the application services explicitly after first boot.
